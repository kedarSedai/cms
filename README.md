# Rising MVPs

- **`frontend/`** — Next.js site (run `npm install` and `npm run dev` here).
- **`backend/`** — Placeholder for Strapi or another API (see `backend/README.md`).

From the repo root you can also run:

```bash
npm run dev
```

which delegates to `frontend`.
