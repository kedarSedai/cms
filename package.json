{
  "name": "rising-mvps",
  "private": true,
  "description": "Monorepo: Next.js app is the `frontend` workspace. Run `npm install` from repo root (installs all workspaces).",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w ryan",
    "build": "npm run build -w ryan",
    "start": "npm run start -w ryan",
    "lint": "npm run lint -w ryan"
  }
}
